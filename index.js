function installIs() {
    throw Error("You may not run install-is directly, installing install-is installs a bunch of other useful packages. See them here: https://www.npmjs.com/package/install-is?activeTab=dependencies");
}
module.exports = installIs;