function installIs() {
    throw Error("You may not run install-is directly, installing install-is installs a bunch of other useful packages. Look at your node_modules folder.");
}
module.exports = installIs;